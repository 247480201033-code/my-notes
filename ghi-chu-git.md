1. git init
Khởi tạo một kho chứa Git (repository) mới ngay tại thư mục hiện tại, cho phép Git bắt đầu theo dõi các thay đổi trong thư mục này.

2. git clone <URL>
Sao chép (tải về) một kho chứa từ trên Internet (như GitHub) về máy tính cá nhân của bạn.

3. git add <tên_file>
Đưa các thay đổi của file vào khu vực chuẩn bị (Staging Area), sẵn sàng để lưu lại ở lần commit tiếp theo (dùng git add . để thêm tất cả file).

4. git commit -m "thông điệp"
Lưu lại phiên bản các thay đổi đã được thêm từ bước git add vào lịch sử của Git kèm theo một tin nhắn mô tả ngắn gọn.

5. git push
Đẩy (tải lên) toàn bộ các commit đã lưu ở máy cục bộ lên kho chứa từ xa (remote repository) trên nền tảng như GitHub.